{
  "name": "bench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench ",
    "build": "napi-raw build --js false --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "workspace:*",
    "tinybench": "^5.0.1",
    "vitest": "^3.2.4"
  }
}
