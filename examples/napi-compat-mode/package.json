{
  "name": "@examples/compat-mode",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "napi-raw build --no-js --features latest",
    "test": "node --import @oxc-node/core/register --test __tests__/**/*.spec.ts"
  },
  "devDependencies": {
    "@napi-rs/cli": "workspace:*",
    "sinon": "^21.0.0"
  }
}
