{
  "name": "bench",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "bench": "vitest bench ",
    "build": "napi-raw build --js false --release"
  },
  "devDependencies": {
    "@napi-rs/cli": "workspace:*",
    "tinybench": "^6.0.2",
    "vitest": "^4.1.8"
  }
}
